{
  "name": "scoped",
  "version": "1.0.0",
  "private": true,
  "description": "Scoped - coding agent workers (Claude Code, Copilot ACP, VS Code Web) + API + CLI",
  "scripts": {
    "build": "pnpm -r build",
    "cli": "tsx packages/cli/src/index.ts",
    "dev:api": "pnpm --filter api dev",
    "dev:portal": "pnpm --filter portal dev",
    "dev:coder-acp-claude-code": "pnpm --filter coder-acp-claude-code dev",
    "dev:coder-acp-copilot": "pnpm --filter coder-acp-copilot dev",
    "docker:up": "docker compose up --build",
    "docker:up:claude-code": "docker compose --profile claude-code up --build",
    "docker:up:copilot": "docker compose --profile copilot up --build",
    "docker:up:vscode-web": "docker compose --profile vscode-web up --build",
    "docker:up:all-workers": "docker compose --profile claude-code --profile copilot --profile vscode-web up --build",
    "docker:up:infra": "docker compose up -d mongodb redis azurite",
    "docker:up:portal": "docker compose --profile portal up --build",
    "docker:down": "docker compose --profile claude-code --profile copilot down",
    "docker:build": "docker compose --profile claude-code --profile copilot build",
    "lint": "pnpm -r lint",
    "test:copilot:concurrent": "concurrently -n req1,req2 -c blue,green \"pnpm cli submit -m 'Request 1: Say hello' -w coder-acp-copilot -u $API_ENDPOINT\" \"pnpm cli submit -m 'Request 2: Say goodbye' -w coder-acp-copilot -u $API_ENDPOINT\"",
    "test:all-coders": "concurrently -n copilot,claude -c blue,magenta \"pnpm cli submit -m 'Hello from Copilot test' -w coder-acp-copilot -u $API_ENDPOINT\" \"pnpm cli submit -m 'Hello from Claude test' -w coder-acp-claude-code -u $API_ENDPOINT\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "keywords": ["azure", "aca", "mongodb", "cosmosdb", "queue", "typescript", "redis", "coding-agents", "acp", "copilot"],
  "license": "MIT",
  "packageManager": "pnpm@10.29.1"
}
