# Pinned versions for the coder-acp-claude-code Docker image.
# This file is the single source of truth — used by:
#   - Dockerfile (via --build-arg, no defaults)
#   - docker-compose.yml (via build args + env vars)
#   - build-acr.sh (sourced for --build-arg)
#   - version-checker-claude-code (parsed via dotenv)
CLAUDE_CODE_ACP_VERSION=0.29.1
CLAUDE_AGENT_SDK_VERSION=0.2.112
