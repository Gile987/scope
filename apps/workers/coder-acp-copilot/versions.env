# Pinned versions for the coder-acp-copilot Docker image.
# This file is the single source of truth — used by:
#   - Dockerfile (via --build-arg, no defaults)
#   - docker-compose.yml (via --env-file)
#   - build-acr.sh (sourced for --build-arg)
#   - Version checker (parsed via dotenv)
COPILOT_CLI_VERSION=0.0.415
