{
  "name": "scope-doc",
  "type": "module",
  "version": "0.0.1",
  "packageManager": "pnpm@11.1.1",
  "scripts": {
    "dev": "worktree-env && astro dev",
    "start": "worktree-env && astro dev",
    "build": "astro build",
    "preview": "worktree-env && astro preview",
    "astro": "astro",
    "refresh:openapi": "test -n \"$SCOPE_OPENAPI_URL\" || { echo 'Set SCOPE_OPENAPI_URL to the URL of the OpenAPI document.' >&2; exit 1; }; curl -fsSL -o src/openapi/scope-openapi.json \"$SCOPE_OPENAPI_URL\" && echo 'Refreshed src/openapi/scope-openapi.json'"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.39.2",
    "astro": "^6.4.6",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "acorn": "^8.16.0",
    "httpsnippet-lite": "^3.0.5",
    "starlight-openapi": "^0.25.1",
    "unist-util-visit": "^5.1.0",
    "worktree-env": "^0.1.0"
  }
}